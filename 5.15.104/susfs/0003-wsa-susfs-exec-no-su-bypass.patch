--- a/fs/exec.c
+++ b/fs/exec.c
@@ -1901,7 +1901,8 @@ static int do_execveat_common(int fd, struct filename *filename,
 		return PTR_ERR(filename);
 
 #ifdef CONFIG_KSU_SUSFS
-	if (likely(susfs_is_current_proc_no_su()))
+	if (likely(susfs_is_current_proc_no_su()) &&
+	    !ksu_sucompat_should_handle_path(filename->name))
 		goto orig_flow;
 
 	if (static_branch_likely(&ksu_su_compat_enabled)) {
