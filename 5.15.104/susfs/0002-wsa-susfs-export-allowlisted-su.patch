--- a/KernelSU/kernel/feature/sucompat.c
+++ b/KernelSU/kernel/feature/sucompat.c
@@ -131,7 +131,7 @@ static bool is_su_path(const char *path)
 }
 static const char ksud_path[] = KSUD_PATH;
 
-static bool ksu_sucompat_should_handle_path(const char *path)
+bool ksu_sucompat_should_handle_path(const char *path)
 {
 #ifdef KSU_COMPAT_USE_STATIC_KEY
     if (!static_branch_unlikely(&ksu_su_compat_enabled))
--- a/KernelSU/kernel/feature/sucompat.h
+++ b/KernelSU/kernel/feature/sucompat.h
@@ -15,6 +15,7 @@ void ksu_sucompat_exit(void);
 
 /* Return a lookup-only replacement for an authorized virtual su path. */
 const char *ksu_sucompat_lookup_redirect(const char *path);
+bool ksu_sucompat_should_handle_path(const char *path);
 
 // Handler functions exported for hook_manager
 #ifdef CONFIG_KSU_SUSFS
--- a/fs/exec.c
+++ b/fs/exec.c
@@ -1886,4 +1886,5 @@ extern int ksu_handle_execveat(int *fd, struct filename **filename_ptr, void *argv,
 			void *envp, int *flags);
 extern int ksu_handle_execveat_sucompat(int *fd, struct filename **filename_ptr, void *argv,
 				void *envp, int *flags);
+extern bool ksu_sucompat_should_handle_path(const char *path);
 #endif
